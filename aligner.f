// ─────────────────────────────────────────────
// rtl.f — Filelist del DUT (cfs_aligner)
// Usa design.v como punto de entrada único —
// ese archivo hace `include de todos los módulos
// en el orden correcto de dependencias.
// ─────────────────────────────────────────────

+incdir+aligner
aligner/design.v